/* this is the custom js file for lucent design iphone X and ipad*/

$(document).ready(() => {
  $('.cards').slick({
    centerMode: true,
    centerPadding: '50px',
    slidesToShow: 1,
    dots: true
  });
});
